# # Задание 1
    оформить снежинку в виде класса
# Задание 2
    сделать так, чтобы после выхода снежинки за границы экрана она появлялась сверху
# Задание 3
    добавить вторую снежинку. Каждая должна иметь собственную скорость падения
# Задание 4
    создать массив из 10 снежинок в случайных местах экрана.
# Задание 5
    изменить количество снежинок до 20
    При нажатии на снежинку она должна исчезнуть.
    Посчитать, за сколько времени в секундах пользователь нажал на все снежинки
    Новая игра должна начинаться при нажатии на клавишу Enter
    
## проверка нажатия кнопки:
``` C++ 
    if (sf::Keyboard::isKeyPressed(sf::Keyboard::Enter)) {
        //действие при нажатии на кнопку  
    }
```
# Задание 6
    Генерировать снежинки в рандомных местах
    в течение 30 секунд.
    Посчитать, за сколько времени пользователь нажал на все снежинки
    Сделать систему рекордов.
    В отдельном файле должен храниться наилучший результат.
    после окончания времени вывести текущий результат и общий рекорд на экране
## добавление текста:
``` C++ 
    //сначала нужно отдельно загрузить шрифт
    sf::Font font;
    font.loadFromFile("/home/konstantin/CLionProjects/Students/Examples/snowflake/ofont.ru_Gravity.ttf");
    //затем задать текст и установить в него шрифт
    sf::Text text;
    text.setFont(font);
    text.setFillColor(sf::Color::Red);//цвет текста
    text.setString(L"привет мир");//установка текста шрифт
    //перемещение и отображения текста осуществляется также, как и для спрайтоа
```